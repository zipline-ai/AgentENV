# T-536: process epoch fence implementation plan r2

Owners: codex-8 (provider) and codex-9 (lifecycle). Plan only; no provider composition or consumer activation is authorized by this document. Review against the app contract at `683610dd4f5c7a4300ba77fffba6a16c51535da6`, `docs/design/lanes/process-epoch-contract.md`. This plan proposes concrete resolutions for pending items 1, 2, 4 and 5. It does not declare that contract's pending list empty.

## Source inventory

Fork baseline: `5fdfcdb`, including async-restore PR #2. Restore operation receipts are separate from execution receipts and grant no process authority. Implementation PR will be rebased on fork main after that prerequisite lands. The tools image currently builds upstream envd tag `2026.17`; the inspected resolved commit is `9c3b7c5dbd181ba819084276b8228f70936e911e`. Pin that full commit and apply a reviewed patch, rather than editing generated clients alone.

| File / anchor | Planned change |
| --- | --- |
| `tools-image/Dockerfile:43–73` | Pin full source commit, apply guest patch, embed source/patch/protocol identity and test the compiled binary. |
| New `tools-image/envd-patches/` and guest patch manifest | Versioned source patch and executable guest tests; patch application must fail on source drift. |
| Upstream `packages/envd/spec/process/process.proto` and generated specs; fork `thirdparty/envd/proto/process.proto` | Add typed epoch authority, exact operation selectors, seal/bind/lookup messages, output coordinates; regenerate Go/Rust clients using existing build tooling. |
| Upstream `internal/services/process/service.go:20,58` | Shared execution gate and exact operation registry; managed requests cannot resolve by PID/tag. |
| Upstream `process/start.go:21,58,62`; `handler/handler.go:423` | Cover ordinary and initialization starts, persist operation claim before spawn, execution admission at actual spawn. |
| Upstream `process/input.go:16,45,63,102`; `handler/handler.go:368,390,410` | Check each input frame and close operation at execution; prevent queued A input from executing after seal. |
| Upstream `process/signal.go`, `update.go`; `handler/handler.go:348,360` | Fence signal and resize, including late work already accepted by the HTTP handler. |
| Upstream `process/connect.go`, `handler/multiplex.go`, `start.go` output forwarding | Bind output to the original process operation/epoch; invalidate stream subscribers at seal. |
| Upstream `internal/api/auth.go:31`; init/config plumbing | Managed mode requires an authenticated controller identity even if legacy access-token setup is absent. An ordinary envd token alone cannot authorize managed mutation. |
| New fork `src/process_epoch.rs`, `src/api/impls/process_epoch.rs`; `src/api/server.rs` | Node-side authenticated descriptor/control/lookup surface, exact-runtime checks and durable receipt mirror. |
| `src/local_store.rs`; new process-epoch tests | Reuse Sync durability and atomic batches in a separate namespace. Do not reuse restore receipts as execution evidence. |
| `src/api/proxy.rs:139,299,712`; `src/sandbox/envd.rs`; `src/orchestrator/service.rs` exact guest access | Saved endpoint only, no automatic resume, managed bypass denial, guest incarnation reconciliation. |
| `services/gateway/internal/`, protocol client generation, fork CI | Exact saved route forwarding, no scheduling fallback, real guest/process/PTY/tmux test job. |
| New node enrollment/trust module and tools-image trust manifest; app issuer/enrollment facets (joint follow-up) | Certificate/manifest verification, monotonic enrollment and trust revision, authenticated response chain. Provisioning and lifecycle facets are release dependencies, not assumed existing capabilities. |

## Proposed protocol and descriptor (pending field 1)

Protocol identifier: `agentenv-process-epoch-v1`. A descriptor contains `protocol`, `node_id`, `node_incarnation`, `runtime_id`, `runtime_incarnation`, `guest_boot_id`, `enrollment_revision`, `process_endpoint`, `guest_build_sha256`, `capabilities_sha256`, and `managed=true`. UUID coordinates use canonical lower-case strings; hashes are lower-case SHA-256 hex. Descriptor and receipt responses are authenticated evidence, not unsigned strings returned by a capability probe.

### Trust chain and roots

The following provisioning is proposed, not present in the current fork. It is a release dependency with tests, not an assumed property of the existing fleet API key.

| Hop / issuer | Source of authority and verification |
| --- | --- |
| Lifecycle → node | A dedicated app `ProcessEpochAuthorityIssuer` signs only a captured, committed dispatch of the exact saved creation/transition. It binds its operation, immutable canonical bytes/hash, tenant/family/sandbox, A and reserved B, launch, route, runtime/incarnations, descriptor digest and expiry. The lifecycle owner must implement the issuer facet; a DTO, browser actor or arbitrary caller string cannot invoke a raw signing endpoint. Read-only lookup authority comes from the saved operation, never a new dispatch claim. |
| Controller → node enrollment | An operator-managed provider enrollment authority issues a short-lived node certificate for the authenticated provisioned VM identity and a newly registered node-process incarnation. Registration uses the VM's workload identity checked against the provider's provisioned instance inventory. The app validates the provider CA, exact node certificate identity, current enrollment revision and saved route through mTLS; neither DNS nor an API-key-authenticated JSON node ID is enough. |
| Node → guest | The node verifies the selected read-only tools artifact against the operator-signed release manifest before mapping it into the exact VM. It creates a per-boot guest channel key/certificate bound to the hypervisor runtime handle, runtime incarnation, boot ID and enrollment revision. Provision over the host-controlled guest-init channel, outside user credentials; restrict enrollment to that handle, never an endpoint from a request. The private channel key is per boot, not baked into a template. The guest checks the enrolled node certificate and the controller signature before admission. |
| Guest → node → lifecycle | Guest receipts are signed with that enrolled guest key, including the complete binding, original request hash, journal revision and receipt body. The node validates the signature, current exact guest enrollment and expected build manifest, fsyncs its mirror, then signs a response envelope binding that guest receipt/digest, node incarnation/revision and the caller's lookup nonce. Lifecycle verifies the provider chain, saved node/guest enrollments, both signatures, nonce, request hash and expected descriptor/build against its saved authority before accepting evidence. Historical first receipts retain their original signatures; a fresh signed lookup envelope reports their current reconciled status without rewriting them. |

This is provider-controlled authentication and verified provisioning, not hardware attestation. The threat model trusts the controller, host/hypervisor and release authority; a compromised host or privileged guest that can replace the managed server is not made trustworthy by a signed self-report. Managed guests cannot retain user-accessible root/sudo paths that replace the verifier, trust bundle, journal or policy. If the current image cannot enforce this boundary, capability advertisement and activation remain blocked pending a reviewed image-policy change. Do not silently claim the current image provides it.

The operator creates separate Ed25519 controller-authorization and provider-enrollment/release roots through the approved secret-management provisioning process. Private roots and controller signing keys live in the controller/operator secret store, never a guest image or user credential volume. Node private keys live in host-only storage; guest boot keys and managed policy are supplied through the host-controlled read-only tools/init channel. App trust bundles pin provider roots and approved manifest hashes; host bundles pin controller and release roots. The guest receives the controller/provider public trust bundle and managed policy from verified read-only tools provisioning. `/init`, restored local configuration and ordinary envd access-token setup cannot unset managed mode or replace that bundle.

Use distinct signed-envelope domains/audiences: `process-epoch/descriptor/v1`, `seal/v1`, `bind/v1`, `lookup/v1`, `initializer/v1`, `mutation/v1`, `stream-open/v1`, `frame/v1`, `guest-receipt/v1`, and `node-response/v1`, each prefixed by `agentenv-process-epoch`. Audience also names the exact receiving node or guest enrollment. Initializer authority binds a saved creation dispatch and a finite approved bootstrap operation set; it cannot bind a funded epoch or act as a general process token. Bind and initializer DTOs still require joint agreement (fields 3/7).

Key rotation is an explicit operator trust-bundle revision: distribute new public keys before issuing with them, retain old verification keys for historical receipts, and require a fresh nonce-bound response from a currently enrolled node. The issuer stops new grants when a key is revoked, and lifecycle rejects current evidence under that revoked enrollment/trust revision. Provider revocation also uses the execution exclusion to close admission and drain or retain admitted work; its acknowledgement is not complete until the exact guest fence is proven. During a lost or incomplete revocation acknowledgement, lifecycle permits no replacement epoch and retains the unresolved work obligation. Unknown or stale revision checks fail closed; the plan does not claim a disconnected guest instantly observes a controller key change. Historical signatures remain audit evidence, never current work authority after revocation. Revoked boot/node keys cannot enroll replacements or mint fresh receipts. Missing keys, unverifiable rotation and expiry retain obligations; they do not reset the epoch or authorize redispatch. No production secret names/values or rollout are assumed by this plan.

Build evidence is pinned independently: the signed release manifest records the full envd source commit, ordered patch hashes, protocol descriptor digest and actual compiled binary/tool-image hashes. The app's approved release policy and node's verified artifact mapping must agree before the guest handshake is considered. The node attests what it provisioned over its authenticated provider identity; the guest's reported build value is checked against that evidence and is never the trust root.

## Seal request, receipt and outcomes (pending field 2)

The proposed immutable binding is: `protocol`, `tenant_id`, `sandbox_family`, `sandbox_id`, `transition_id`, `launch_id`, `expected_session_id`, `reserved_session_id`, all descriptor coordinates, and `operation_id`. Canonical request bytes are a fixed-order UTF-8 JSON object with no maps, unknown fields, alternate UUID spellings or duplicate keys. Store the exact original bytes and SHA-256; golden vectors define encoding across implementations. The outer signed envelope carries that digest; no circular hash field occurs inside its own input. A changed byte sequence under the same operation ID conflicts.

`SealProcessEpoch` closes admission for expected A under the same gate used at final execution. Under that exclusion, close new admissions and freeze the complete admitted process/mutation/stream inventory and its digest. Persist closing intent before acknowledging receipt of the close request. That acknowledgement is only `accepted/incomplete`, never sealed/drained success; queued operations must reacquire/check admission, not wait through the seal and execute in B. Track all admitted work until it finishes or is conclusively canceled. No SQL lock or provider network call is part of this guest exclusion.

Receipt fields: immutable binding, `request_sha256`, `receipt_id`, `state` (`sealed` or `incomplete`), `execution_outcome` (`drained`, `scope_retired`, `unknown`), and `affected_operations_sha256`. The digest covers the sorted exact process/mutation/stream inventory frozen under the execution exclusion, not PIDs. No admission can occur between closing the gate and freezing that inventory. A successful receipt requires no unresolved admitted work or effects in that inventory. Persist the first completed receipt before returning it. An incomplete observation is not a successful receipt and cannot authorize bind. Later lookup may observe a subsequently proven terminal receipt; it cannot rewrite one already committed.

A successful stdin write, tcdrain, empty socket buffer or closed stream does not establish that a program consumed PTY input. V1 will conservatively return `incomplete/unknown` for any affected live scope with unproved queued input or continuing effects. It will not kill processes to obtain success. `scope_retired` requires separate explicit captured retirement authority plus real proof that the exact affected process scope has ended; siblings and history are preserved. Processes that escape the provable scope keep the result unknown. This deliberately leaves retained interactive-process handover unavailable until codex-9 and the reviewer approve an implementable retirement/drain policy.

## Lookup, storage and restart (pending field 4)

Proposed node endpoints: POST `/sandboxes/{runtime_id}/process-epochs/seal`, POST `/sandboxes/{runtime_id}/process-epochs/bind`, and GET `/sandboxes/{runtime_id}/process-epoch-operations/{operation_id}`. Guest controls use typed ProcessEpoch RPCs with the identical authority binding. The node checks the saved runtime/incarnation and forwards only to the saved endpoint. Neither node nor gateway may schedule, resume, resolve a newer runtime or redispatch a mutation to answer lookup.

### Durable records and authority outside guest snapshots

The authoritative non-rollback anchor is the node's fsynced host `process_epoch` ledger, outside all guest disks/memory snapshots. Its key is `(controller_identity, tenant_id, runtime_id, runtime_incarnation)`, with monotonically increasing `enrollment_revision`, current node/guest incarnations, current epoch/phase, frozen inventory digest and append-only operation/receipt hashes. An operation key adds `(operation_id, request_sha256)`; a stream-frame key adds `(stream_id, sequence)`. Accepted identities remain tombstones indefinitely. Loss or corruption of this ledger is unavailability, not first enrollment. No automatic host-ledger reset or restore from an older backup is allowed.

Lifecycle also retains the saved enrollment revision and receipt digest with its transition. A node mirror older than that saved anchor cannot be accepted. Rebuilding a lost host ledger requires a separately reviewed evidence recovery procedure; it is unavailable in this cut. Neither a guest snapshot nor a restarted node may manufacture its replacement. Operator restoration of host metadata without a proven current anchor keeps the node quarantined.

The guest has an fsynced journal keyed by the same binding and enrolled boot revision. A per-runtime reconciliation mutex serializes node seal/bind/enrollment/lookup state transitions. Guest execution exclusion serializes local reconciliation with admission and effects. Do not hold the guest exclusion over node network calls: publish closed state locally, release the lock, collect external evidence, then conditionally install only if the captured revision and closed phase still match. Competing reconciliation attempts CAS the same external revision; only one can publish a new enrollment. The loser returns the saved result or unavailable, never a second operation.

### Transition and fsync order

| State / transition | Required ordering and response |
| --- | --- |
| `unreconciled` | Every guest/server startup and restored instance starts unavailable. Node refuses process forwarding until exact enrollment reconciles. A snapshot known to contain an open gate or unresolved process effects is not eligible for managed restore; no claim is made that network isolation stops already queued PTY commands. Only a proven closed/quiesced snapshot can enter this path. |
| `open(A)` → `closing(A, transition, inventory)` | Node first fsyncs the exact close intent against current enrollment/A. Guest closes admission and freezes inventory under exclusion, fsyncs closing intent, then returns accepted/incomplete. Node fsyncs that observation. A timeout never returns either copy to open. |
| Mutation `accepted` → `execution_claimed` | Node fsyncs the immutable operation/frame identity and grant against open A before delivery. Guest fsyncs the matching one-use claim, then rechecks binding and gate at the effect boundary. The operation remains in the frozen seal inventory if it was admitted before closing. |
| `execution_claimed` → `effect_unknown` or `completed` | After an effect, guest fsyncs exact completion evidence before reply; node fsyncs its mirror before acknowledging completion. Any crash/partial effect without conclusive completion records effect_unknown. A claimed operation is never re-executed to discover its outcome. A completed byte write alone does not prove PTY drain. |
| `closing` → `completed_seal` | Guest proves every frozen inventory member drained or separately authorized and conclusively retired, fsyncs the first signed terminal receipt, then node validates and fsyncs the exact receipt mirror. Only the mirrored authenticated terminal receipt can be returned as successful seal. Incomplete is never promotable by a timeout or by node inference. |
| `completed_seal` → `bind_pending(B)` → `bound(B)` | Node fsyncs bind intent conditioned on exact A/transition/completed-seal receipt. Guest verifies that intent, fsyncs its conditional binding and receipt while still closed. Node fsyncs the identical receipt and bound revision. Guest remains bound-closed after the node commit acknowledgement. Only the separate post-install ReleaseEpoch authority described in the fields 3/7 draft may open process admission; lost acknowledgement leaves it closed. This provider bind state does not publish a billable B. |
| Receipt lookup | Returns the first immutable completed receipt only when current enrollment reconciliation supports it, inside a fresh authenticated response. Claimed/closing/partial state returns accepted/incomplete with no usable seal/bind receipt. A genuinely absent exact operation returns fixed 404. Missing/corrupt authority storage or mismatched copies returns unavailable/unknown, not 404. All are read/evidence recovery paths, never effect redispatch. |

Fresh boot enrollment is node-controlled. Before incrementing the external enrollment revision, the node must prove the old exact VM/process-server incarnation cannot execute or accept work; unknown stop/fence evidence blocks enrollment. Bind the new host-injected channel identity to the saved exact runtime/boot and approved tools artifact. Fence prior channels and trust revisions; reconcile the closed guest journal to the external ledger before publishing the new descriptor. A fresh boot UUID by itself is not proof that the older boot stopped. This enrollment requires the lifecycle owner's captured transition/creation authority and is not permitted to create a new funded session.

### Restart / rollback matrix

| Observed copies | Recovery and lookup result |
| --- | --- |
| Guest completion ahead of node mirror | Read the exact signed guest receipt under the existing external claim, verify the current boot and hash chain, then CAS/fsync the missing mirror. Return the same receipt; never rerun its operation. |
| Node intent/claim ahead of guest | Keep closed or mark effect_unknown. Absence of guest claim is not proof no effect occurred. Return incomplete; do not deliver the mutation again. |
| Node completed receipt ahead of guest journal | Treat as rollback/corruption, quarantine the guest and return unknown. Never copy bound B into a stale guest and open it. Historical node evidence is retained for authorized recovery. |
| Lost claim or receipt reply | Exact lookup returns the durable state/first receipt. Lost claim with no conclusive receipt stays unknown; no new key, frame or operation is minted. |
| Snapshot rollback after B | External enrollment/epoch/journal revision exceeds snapshot state: refuse managed restore/publication. Keep the external B record and obligation; no reopening A, resetting the journal or inventing C. |
| Missing/corrupt guest or host storage | Quarantine and preserve whatever evidence exists; no success, initial bind or journal reset. Exact recovery outside this cut is required. |
| Two concurrent reconciliations | Both start closed; serialize/CAS the same external enrollment revision and guest closed revision. One commits; the other returns identical proven evidence or unavailable. Neither performs new effects. |

Crash tests stop real processes between every fsync, provider delivery, effect, guest completion, node mirror and guest publication boundary. Restart with the same files and with older guest snapshot files; assert the matrix outcomes, immutable receipts and zero duplicate effects. Scope retirement after unknown remains a separately authorized operation, not a lookup side effect.

## Execution and bind fencing (pending field 5)

Each process has a unique `process_operation_id`; each unary mutation has its own `operation_id`, immutable digest and epoch binding. Registry lookup is exact, never PID/tag search. Seal and final spawn/write/signal/resize/close share exclusion. Long writes must support cancellation and bounded drain; failure to prove cancellation leaves the epoch closed and incomplete. The seal transport deadline cannot turn incomplete work into success or reopen A.

A stream first performs authenticated `OpenProcessStream` admission. Its immutable record binds `stream_id`, tenant/family/sandbox, node/runtime/boot enrollment, funded epoch, exact process operation, terminal incarnation, permitted direction/operation (`stdin`, `pty-input`, or `output-only`), and the captured opening authority/hash. Both the opening and every subsequent frame must be authorized; a frame token cannot create a stream. Reconnecting references the same saved stream record and epoch, never migrates A to B. Output-only Connect receives explicit captured read authority and cannot gain mutation authority from successful attachment.

Frames contain the original stream ID, monotonically increasing sequence starting at 1, immutable mutation ID, payload length and payload digest. Persist acceptance and the next expected sequence before execution. Only the next frame may execute, under the original binding and current gate; a gap/reordering is denied without writing. A duplicate sequence with identical bytes returns its first durable outcome without another write; changed bytes or mutation ID conflicts. Keep claimed/completed frame records, including empty or keepalive frames as non-effect protocol messages with their permitted type, so a reconnect cannot reset ordering.

A partial write makes that frame `effect_unknown`, closes input admission for the stream, and blocks all later sequences. Report no success or consumed-byte inference. Retrying cannot split, reframe or assign new identities to its old payload: the node/guest only accept grants for the saved outstanding frame; captured client recovery reuses it. Opening a replacement stream/process to bypass unresolved input requires separately reviewed lifecycle authority and conclusive retirement, not a generic retry option. Identical text intentionally sent later is not deduplicated by content; its new authority must be independently granted after the prior operation is resolved.

Every frame reproves the immutable open binding immediately before its write while holding execution exclusion. Seal closes stream admission, freezes accepted frame inventory and invalidates output subscriptions under that same exclusion. Frames queued before seal but not admitted fail; admitted partial writes keep seal incomplete. A reconnect cannot re-enable them in B.

Output chunks carry original epoch/process/terminal/stream coordinates plus sequence. Authorization applies at output-only open and before forwarding each chunk; seal invalidates the subscriber generation under the execution exclusion, so later queued chunks are dropped. Socket send acceptance before seal is already-delivered history, not revocable output. Node and app completion checks reject late A chunks before disclosure; the app discards its unforwarded A buffer and never replays it through B. No history rewrite is attempted. Do not hold the exclusion across an indefinitely blocking network flush: bounded subscriber queues register admission at forwarding, cancellation closes them at seal, and any unproved in-flight disclosure remains unavailable to the B consumer.

`BindProcessEpoch` performs an atomic expected-A/transition/seal-receipt comparison before publishing reserved B. No queued A operation is carried into B. Exact replay returns the original receipt; changed scope or seal conflicts. Initial bind is a separate operation requiring saved creation authority and an unused exact guest incarnation. The request/receipt fields for first bind and the lifecycle publication transaction are pending joint contract items 3 and 7; implement no success stub while those are unresolved.

Managed guests deny the old PID/tag process mutation paths even through direct envd access. Audit initializer/bootstrap process starts as separate authority: managed boot cannot silently use an unrestricted legacy initialization path. Ordinary unmanaged guests keep their current protocol. Output chunks retain epoch, process operation and terminal incarnation; seal cancels the old subscribers. App-buffered output invalidation is codex-9's participant and remains required before consumers activate.

## Implementation and tests

One fork implementation PR with reviewable commits: (1) pinned source/build manifest and typed protocol with invalid-descriptor tests; (2) durable guest operation registry plus seal/bind exclusion and real process tests; (3) authenticated exact node/gateway forwarding, restart reconciliation and hard CI job. If the guest patch cannot be reviewed as one PR, split after the dormant protocol/registry commit; neither partial cut advertises a usable capability. App registration/composition is a later jointly reviewed PR after the contract pending list is empty.

Write red tests before enforcement. These named proofs from the versioned contract are commitments, not existing passing tests:

- `TestProcessEpochSealOrdersEveryMutation`: real child processes and PTYs; barriers before admission and immediately before the actual effect; both orders for spawn, stdin, signal, resize, close, and queued stream frames. Assert exact operation IDs and zero denied effects.
- `TestProcessEpochUnknownPTYDrainRefusesBind`: unread PTY bytes and blocked writes keep B unavailable; exact tmux sibling remains alive and history unchanged. No sleep-based ordering or tcdrain inference.
- `TestProcessEpochExactOperationReplay`: same operation returns first receipt; changed bytes/scope/epoch/incarnation denied; kill server after claim and after effect, reopen journal, lose replies, and prove no duplicate spawn or PID fallback.
- `TestProcessEpochRestoredGuestAndProxyBypassDeny`: wrong build, unsupported guest, stale snapshot ledger, direct envd, proxy host/path and gateway fallback all deny; zero resume/scheduler calls.
- `TestProcessEpochOutputDoesNotCrossSeal`: real output-only Connect and input-stream barriers, hold output before forwarding across seal, then release; no A chunk reaches B, prior displayed history retained. Codex-9 supplies the app half.
- `TestProcessEpochStreamReplayAndPartialWrite`: real pipe/PTY capacity barrier forces partial write; exact duplicate sequence never writes twice, changed bytes and reordered frames deny, seal between accepted frames rejects queued input, lost acknowledgements cannot reframe or reopen the stream.
- `TestProcessEpochTrustChainDenials`: real signed request/response fixtures with missing/rotated/revoked keys, wrong node/boot/audience, forged descriptor/receipt, mismatched independently pinned artifact, and restored writable trust material. All make zero managed effects and no funded publication.
- `TestProcessEpochRestartReconciliationMatrix`: crash at each durable/effect boundary in the table, restore older journal files, corrupt/remove each ledger, race two reconciliations; exact receipt stays immutable and mismatches never enable work.

CI must execute the patched guest binary with real process/PTY/tmux fixtures, not only mocked Rust clients. Run guest Go tests with race detection, Rust build/tests/clippy, gateway tests/race, protocol generation drift checks and image build. Fork Actions' pending admin opt-in blocks PR READY, not this plan. Local logs are interim evidence. Build/import/select and live operations remain coordinator-owned.

## Contract and release blockers

Before provider composition, publish the reviewed DTO/golden vectors, descriptor authentication/trust provisioning, external enrollment anchor and exact first-receipt lookup semantics into the jointly owned versioned app contract. This r2 is a proposed implementation contract, not either owner's confirmation that pending fields are closed. Obtain codex-9 confirmation of saved authority, first bind, install/replay/neutral recovery and explicit process retirement policy. Do not remove pending rows merely because this plan proposes fields.

Release additionally requires codex-9's real-PG `TestFundedEpochRequiresExactSealBindReceipt`, `TestFundedEpochReceiptReproofDuringProviderBarrier`, and `TestFundedEpochNeutralReceiptRecovery`, combined provider barriers, tested guest/node/gateway image digests, compatible rollout approval and exact-head CI. No billable session is invented from a provider receipt, and no current session is inferred from runtime generation. #125/#127 boot bounds/markers and #134 retained-runtime cancellation/accounting contracts remain unchanged.

## Joint fields 3/7: provider draft for codex-9

This section is the provider proposal, not a jointly confirmed contract. It sharpens the earlier bind row: a provider bind receipt means **bound but execution closed**. Opening a funded epoch requires a separate release proof from the lifecycle transaction that actually installed it. Neither a reserved session nor a successful provider bind is evidence of funding. Codex-9 must confirm the named authority predicates and transaction participants before these shapes are final.

### Common encoding and identity

All DTOs use the r2 fixed-order canonical JSON and signed-envelope domains. UUIDs are canonical strings, revision/sequence counters are nonnegative integers, hashes are SHA-256 hex, and absent optional fields encode as omitted fields, never null. Unknown/duplicate fields and noncanonical bytes are rejected. Golden vectors cover initial bind, A→B, restored LR→C, missing fields, changed hashes and every incomplete outcome. The unique ledger key is `(controller, tenant, runtime_id, runtime_incarnation, operation_id)`; `request_sha256` is an immutable column checked for conflict, never an extra key permitting reuse of an operation ID.

`EpochBinding` fields, in order: `protocol`, `tenant_id`, `sandbox_family`, `sandbox_id`, `transition_id`, `launch_id`, `operation_id`, `reserved_session_id`, `descriptor_sha256`, `node_id`, `node_incarnation`, `runtime_id`, `runtime_incarnation`, `guest_boot_id`, `enrollment_revision`, `process_endpoint`, `guest_build_sha256`, `capabilities_sha256`. An authenticated saved descriptor supplies all provider coordinates; client choices do not override it. `sandbox_family` is explicit (`legacy` or `native`), and no implementation for one family grants the other.

`SavedDispatchProof` fields: `dispatch_id`, `dispatch_request_sha256`, `dispatch_kind`, `creation_or_transition_id`, `launch_id`, `reserved_session_id`, `authority_revision`, `exact_result_id`, `exact_result_sha256`. The issuer consumes a locked, committed lifecycle record with these bindings; it does not sign these fields from an HTTP request or DTO. The outer authority envelope includes signer/key/trust revision, operation-specific audience, descriptor binding, request digest and expiry. Lookup has its own saved-operation authority and nonce; it does not reuse dispatch permission.

### Initial creation

`InitialBindRequest` fields: `binding`, `saved_dispatch`, `adoption_id`, `adoption_revision`, `adoption_sha256`. It has no expected-A field or seal receipt. The lifecycle issuer must prove: exact saved creation identity and frozen tenant/owner/family/request/route/allocation; a single claimed dispatch; exact result evidence for this runtime/incarnation; adoption/live-edge committed for that result; and the same reserved session captured before dispatch. An operator-chosen runtime ID or a reservation alone fails these predicates. Codex-9 owns the exact private creation/adoption proof participant; its public projection cannot itself mint authority.

The node independently verifies its original exact creation/restore result and enrollment against that saved dispatch/result, and requires an unbound, execution-closed enrollment whose initial-bind slot has never been consumed. A replacement guest/runtime is not a retry of the original proof. A restored guest with historical epochs cannot use initial creation to erase them. Managed initializer operations have separate finite initializer authority and never imply initial bind or tenant-command permission. Ordinary tenant process access remains closed throughout.

### Transition bind

`TransitionBindRequest` fields: `binding`, `saved_dispatch`, `expected_session_id`, `seal_operation_id`, `seal_request_sha256`, `seal_receipt_id`, `seal_receipt_sha256`, `sealed_inventory_sha256`, `prior_enrollment_revision`. The provider requires its exact first completed seal receipt, matching tenant/family/sandbox/transition/A/launch/reserved-B and affected inventory. An accepted/incomplete close response cannot bind. `expected_session_id` must equal the externally anchored current epoch; a runtime generation or caller-selected A cannot substitute. A restore with a different runtime or boot needs the explicitly saved restore transition and reconciled exact enrollment; it cannot borrow a seal from another runtime to authorize C.

For A→B, the completed seal must cover the same runtime enrollment. For LR→C, `saved_dispatch.exact_result_id/hash` additionally names the exact restored-runtime result, while the seal reference names the saved predecessor retirement/quiescence evidence. Codex-9 must confirm this cross-incarnation restore proof or keep that arm unavailable; the provider will not treat a same-sandbox label as the bridge. The binding transition must explicitly connect predecessor and result enrollments in the signed canonical authority.

### Receipts and release

`BindReceipt` fields: `binding`, `request_sha256`, `receipt_id`, `kind` (`initial` or `transition`), `state` (`bound_closed` or `incomplete`), `expected_session_id` (transition only), `seal_receipt_sha256` (transition only), `adoption_sha256` (initial only), `node_ledger_revision`, `guest_journal_revision`. Only `bound_closed` is eligible evidence for conditional lifecycle installation; it is never permission to execute. The first terminal receipt is immutable and authenticated by the response chain. Duplicate request bytes return that receipt, and changed scope/bytes conflict. Failure, unknown state or a lost reply retains the closed epoch and exact obligation.

`ReleaseEpochRequest` fields: `binding`, `bind_receipt_id`, `bind_receipt_sha256`, `installed_session_id`, `installation_id`, `installation_revision`, `installation_sha256`, `access_grant_id`, `access_grant_sha256`, `lease_not_after`. Use a separate `process-epoch/release/v1` audience. The issuer must consume the committed installed/funded session and exact current authorized access grant, not a reserved row, user DTO or uncommitted callback. `installed_session_id` must equal the bound reserved ID. The lease is bounded by the lifecycle grant; timeout, missing renewal or revoked enrollment closes admission, never issues another session or restores A.

The provider CASes only that matching bound-closed enrollment to released under its gate after recording release authority durably. `ReleaseReceipt` binds the release request/hash and ledger revision; lost replies recover it by exact lookup without issuing another bind or release identity. A release retry cannot change the grant or extend its deadline; renewal is a separately authorized revision. Implementation of renewal and revocation must preserve execution/drain and host accounting rules and remains part of the joint publication contract.

### Provider/lifecycle order and proof obligations

1. **Lifecycle reserve/claim:** codex-9 persists the creation or transition, frozen request/route/allocation and reserved session before provider dispatch. A short ordered transaction authorizes the exact dispatch; no SQL locks span I/O. Pending work retains the existing accounting/cleanup obligation; the provider does not create a billable session.
2. **Exact result and closed enrollment:** node records exact result and enrolls a verified closed process endpoint. Initial creation requires later adoption proof; transition requires completed seal and, for restore, the explicit predecessor/result bridge. All guest observations are subject to the root-tenant threat-model section below.
3. **Provider bind:** validate saved authority, persist bind intent, guest closed binding, node mirror, and immutable `bound_closed` receipt. Do not open process access when acknowledging this receipt.
4. **Lifecycle install:** under the owner's retained lock order, reprove saved authority, exact result/adoption or transition, receipt/enrollment and current lifecycle/accounting conditions. Conditionally install B/C and record the first bind receipt in the same transaction. A failed reproof leaves the provider closed and retains cleanup evidence; it does not synthesize an active actor or another debit/session.
5. **Authorized release:** after commit, the issuer rereads/proves the exact installed session plus current authorized access grant and signs release. Node durably validates/CASes the bound-closed enrollment before admission. Lost SQL connection before proven commit cannot issue release; ambiguous provider release retains exact lookup and accounting obligations.
6. **Completion and later close:** lifecycle revalidates the exact release result before exposing a usable endpoint. Member deactivation or replacement during I/O denies new access and triggers the existing captured close/settlement obligation; it never retroactively assumes an already released runtime did no work. Provider fences/drains the exact enrollment before close is considered conclusive. Neutral recovery uses saved transition/receipt authority and cannot mint a user access grant.

Joint tests: initial proof missing claim/result/adoption returns zero provider effects; wrong family/tenant/runtime/boot/seal/inventory denied; reservation and bind receipt without committed funding give zero input/spawn; barriers before/after bind mirror, lifecycle install and release prove no early process admission; connection loss preserves full rows/ledger and first receipts; deactivation/replacement in both orders preserves obligations and never signs fresh active-user authority; duplicate release cannot extend the lease. These extend the contract's three real-PG funded-epoch tests and real provider barriers, not mocks in place of those tests.

**Requested codex-9 confirmation:** exact private issuer/creation/adoption/installation facets, restore predecessor/result proof, release/access-grant issuance, lease/renewal/close accounting ordering and neutral recovery predicates. Until confirmed in a committed app contract, fields 3/7 remain pending and provider composition remains prohibited.

## Root-tenant threat model and host requirements (review before implementation)

**The current sandbox user has root/sudo. Therefore the guest verifier, boot key, journal, epoch state, process inventory and reported drain are tenant-controlled.** Read-only provisioning and a verified initial binary do not prevent root from replacing runtime behavior, reading keys or fabricating later results. This section overrides any earlier implication that a guest signature or a node signature wrapping it proves trustworthy execution/drain. Guest authentication identifies the enrolled endpoint; it does not attest its honesty. No hardware attestation is claimed.

### Attacker capabilities and gains

Assume a tenant controls its authorized guest completely: arbitrary code/kernel-visible state, process inputs and outputs, verifier patching, guest key extraction, journal rollback, deliberate duplicate command execution, forged guest receipts, replayed network requests and fabricated PTY-drain claims. It does not control the host/hypervisor, node keys/ledger, lifecycle database/signing keys or another sandbox's host allocation. Host escape or a compromised provider is outside this threat model, not something guest protocol signing can repair.

| Tampering | Tenant gain if we trust it | Required boundary |
| --- | --- | --- |
| Disable guest epoch/frame checks or replay PTY input | Arbitrary/duplicate execution in its sandbox, including commands it can already run as root. In a shared sandbox this can also harm other authorized users' work; do not describe all consequences as harmless to the actor alone. | Guest-side exactly-once/drain is a cooperative correctness property only. Host must still prevent additional platform dispatches and enforce paid runtime lifetime. |
| Forge sealed/drained/bound receipts with an extracted guest key | Could falsely unblock B, carry old execution into a new funded epoch, evade a required stop or cause lifecycle to settle A while work continues. This is more than self-harm and is unacceptable. | Node must never certify execution cessation based solely on guest data; lifecycle accepts only node-signed receipts backed by independently established host predicates. |
| Forge guest boot/build strings or restore an older journal | Could reopen stale authority or masquerade as a clean initial bind. | Host-owned enrollment, actual runtime handle/incarnation, approved artifact mapping and non-rollback ledger; no reset or current authority from a guest string. |
| Run workloads after a fake close or before release | Could consume unmetered compute even if managed RPCs are denied. | Hypervisor/runtime CPU lifecycle plus lifecycle accounting, not the process API, enforces the compute boundary. A denied guest RPC is not proof the VM stopped computing. |
| Forge input/output completion | Can corrupt its own sandbox's results and mislead collaborators; could otherwise replay old output into a trusted current broker. | Treat content as tenant-produced. Host/app enforce provenance coordinates and broker epochs; a guest claim cannot change scope or authorize disclosure. |
| Present another tenant/runtime in a request or receipt | Attempts cross-tenant process access, billing mutation or credential release. | Node independently binds authenticated controller authority to host allocation and immutable operation; lifecycle reproofs exact tenant/family/session. Guest channel keys are scoped to one enrollment and cannot sign controller or node authority. |
| Extract a guest key, envd token or local credential | Compromises material legitimately delivered to that guest, not a global node/controller signing key. | No host, controller or cross-tenant credentials are mounted in the guest. Provider control endpoints reject guest credentials. Existing external credential brokers must retain their own scoped proof/revocation checks. |

No guest-signed field may by itself create an extra VM, fund B, settle A, release another tenant's credentials, widen a route, or erase an operation. A node signature is necessary but insufficient if the node merely re-signs an untrusted claim. The receipt must identify the exact host predicate/evidence it certifies. A fake descriptor that passes a guest handshake cannot establish a build or allocation different from the independently verified host inventory.

### Host/lifecycle anchors and receipt classes

The node owns operation uniqueness and immutable request hashes, routing, admission queues, stream/frame sequence and forwarding records, enrolled runtime handles, trust revisions and the durable mirror. The host ledger admits only controller-authorized saved operations; a guest cannot allocate new operation IDs with billing authority. For each network mutation the node records the exact once-only dispatch before forwarding. Lost/partial guest delivery remains unknown and is never resent or reframed under a new ID. This proves platform dispatch behavior, not that malicious root executes a command only once.

Receipts must distinguish `host_dispatch_recorded`, `guest_reported`, and `host_scope_stopped`. `guest_reported` completion can inform UI/correctness but cannot satisfy seal/drain or accounting release predicates. An authenticated node response preserves that classification and the exact backing evidence; no promotion by mirroring/signing. The fields 3/7 joint contract must reflect this distinction before it is confirmed.

A successful security-relevant seal for a root-capable guest requires a host-controlled execution fence for the complete affected isolation domain. The conservative deployable mitigation is: keep admission closed, retain the existing accounting obligation, and refuse transition bind/release until the node proves the exact old VM/isolation domain is stopped under separately captured lifecycle authority. A stop acknowledgement must be backed by the hypervisor handle/process termination or equivalent conclusive host evidence, including no second copy of the incarnation. Unknown stop or lost evidence retains obligations and does not permit replacement. Suspending an open guest and later resuming its memory is not such a proof: buffered A work could execute after wake.

This mitigation cannot silently retire sibling processes or the whole sandbox to satisfy a per-process seal. If the authorized scope excludes siblings in the same VM, a whole-VM stop is not permitted and successful handover remains unavailable. A future finer-grained host isolation domain or removal of tenant privilege needs a separate reviewed policy/design; neither is assumed here. Retained warm terminal handover stays blocked under the present root/sudo model. Guest journal/drain checks can improve behavior for cooperative guests but cannot remove that blocker.

For an approved whole-sandbox retirement, a subsequent epoch requires a separately authorized fresh exact runtime/boot and lifecycle reservation/install/release sequence. Do not resume old open memory as a substitute. Persistent tenant data may intentionally affect later commands; the system does not promise that hostile root cannot write a script which repeats work. Every VM that can execute such work must nevertheless be covered by the host/lifecycle accounting obligation. A host-paused pending runtime can be released only after committed funding; where provider bootstrap needs CPU first, its existing explicit creation/dispatch accounting obligation must cover that work. Codex-9 must confirm that accounting composition; a new process protocol cannot invent an unbilled grace period.

Lifecycle remains the sole billing/session authority. It installs B only from exact saved operation and independently validated provider evidence, settles A only after its required host stop/close proof, and issues release only from the committed funded installation and current grant. Guest-root falsification cannot cause an early billing stop or create a second funded session. Revocation closes node admission; accounting continues for possibly running work until exact host cessation is proven. No read from the guest, including its process list, can substitute for this evidence.

### Required adversarial proofs

- Patch the real guest verifier to return arbitrary sealed/bound success and use its extracted signing key: node/lifecycle must reject it as a host drain/stop proof, keep B closed and retain A's accounting obligation.
- Roll back the guest journal, change build/boot IDs and replay old receipts: host enrollment and immutable ledger remain unchanged; no new bind, dispatch or settlement occurs.
- Run a root workload while reporting no processes/ready-to-stop: only conclusive host termination allows close/settlement. Lost stop response retains the exact obligation, never authorizes another runtime.
- Present valid guest signatures for foreign tenant/runtime/operation/audience and attempt provider-control access: zero foreign process/credential/billing effects, with exact target assertions.
- Exercise cooperative per-frame/PTY barriers as planned, but label their execution/drain guarantees conditional on the guest implementation. Add host forwarding tests proving one dispatch despite fabricated duplicate acknowledgements and partial delivery.
- Preserve unrelated tmux sessions when the requested authority is process-scoped: return incomplete, do not substitute whole-VM retirement. Whole-sandbox retirement tests require separately captured authority covering that domain.

**Decision requested before implementation:** codex-7 reviews this host-anchored security boundary and its explicit loss of warm per-process handover for root guests. Codex-9 must incorporate receipt classes, host stop predicates and accounting/release ordering into joint fields 3/7. Until that agreement is committed, the pending list is not empty, no provider composition or capability advertisement is permitted, and the earlier guest-only sealed-success path is not a security-valid implementation target.

## Provider reply to lifecycle reconciliation 6616de05

Reviewed both `Joint reconciliation with provider b93236a6` and `Lifecycle proposal v0.2: joint fields 3 and 7` at app commit `6616de05`. The eight replies below adopt the lifecycle positions as clarified here. They supersede conflicting wording in the earlier provider draft; they do not confirm an empty pending list or authorize composition. The root-tenant threat model at `26fb20cd` still requires codex-7 review and must be incorporated before guest-derived success can be trusted.

| Row | Reply | Adopted fields / ordering |
| --- | --- | --- |
| 1. Bind publication | **AGREE** | Only a node-mirrored, authenticated `bound_closed` receipt can be consumed for conditional B/C publication. Provider bind never opens execution. Committed lifecycle publication precedes a separately claimed, signed ReleaseEpoch operation; each later process/stream grant also requires current publication. Remove all earlier open-on-bind language from the operative contract. |
| 2. First bind after adoption | **AGREE** | Initial bind requires the committed exact adoption/live edge and immutable adoption proof from `captureInitialProcessAdoptionProofTx` joining `adoptLegacyCreationTx`. Adoption grants no process access. Bind failure retains the registered row and captured cleanup; it does not trigger a guessed unlink or delete a replacement. The proof participant remains to be implemented and tested. |
| 3. Initial launch field and owner | **AGREE** | Add `authority_kind=initial_creation|epoch_transition` to the canonical tagged request. Omit `launch_id` in the initial arm, and reject it if supplied; require L/LR in the transition arm. Initial authority requires `creation_id`, `owner_user_id`, creation generation, saved provider operation key/request hash, reserved session and exact result binding. `transition_id` in the initial common envelope references that same durable creation identity, not a fabricated transition/launch record; require equality with `creation_id`. For transitions the saved proof digest binds original owner and the issuer revalidates it; no current-user substitution. Unknown or mixed arms fail golden-vector validation. |
| 4. Adoption/installation versus billing start | **AGREE** | P1 billing start is the first committed DB observation after exact running confirmation, under the existing evidence writer. Creation process installation publishes access against that already-open exact session; adoption/bind/release neither defer its start nor reopen it. Wake/restore preserve separately committed running evidence and the existing receipt-gated B/C installation. Running while bound-closed remains chargeable; provider timestamps, receipt timestamps, lease expiry and process availability are not start/stop clocks. |
| 5. Restore predecessor/result bridge | **AGREE** | Define an explicit signed `restore_bridge` in the epoch-transition arm as detailed below. It binds predecessor descriptor/enrollment/A and exact first seal operation/request/receipt/inventory to LR's immutable request/result and destination descriptor/C. No field is inferred from a sandbox label, VM absence or P2 transfer. Until exact proof and real provider/PG tests exist, restore process binding stays unavailable. No new alternative retirement-proof arm is implicitly added. |
| 6. Release/renewal | **AGREE** | `claimProcessEpochReleaseTx` locks/reproves the installed session/publication, current grant and trust revision, then persists the immutable request, operation ID and fixed deadline before signing after commit. Deadline comes from the committed access-grant policy, with no invented duration. Renewal is a new authorized operation/revision tied to the prior release receipt; it cannot mutate an old request, reopen a revoked epoch or bypass unknown release/drain. Expiry closes process admission, not compute accounting. Concrete revision/expiry fields below are the provider proposal for the joint text. |
| 7. Release race / neutral recovery | **AGREE** | Persist claim before transport and retain first evidence after I/O; reprove current publication/grant before exposing the endpoint. Neutral authority can look up saved evidence and perform already-captured cleanup, never issue/renew a user grant or mint a release claim. Provider revocation and release share exact enrollment/revision ordering; delayed lower-revision release is denied, while an already admitted release requires conclusive fencing or a retained unresolved obligation. SQL denial alone is not a provider fence. |
| 8. Operation uniqueness | **AGREE** | Key by authenticated controller, tenant, exact runtime/incarnation and operation ID. Store one immutable request hash as a conflicting value, not an extra key dimension. A changed hash is a conflict with zero new effects; byte-identical retry/lookup returns the first saved outcome. Retain tombstones and test both request bodies under the same ID. |

### Concrete bridge and release fields for joint text

`restore_bridge` is required only for a restore transition and forbidden for initial creation and same-runtime wake. In canonical order it contains: `predecessor_descriptor_sha256`, `predecessor_runtime_id`, `predecessor_runtime_incarnation`, `predecessor_guest_boot_id`, `predecessor_enrollment_revision`, `expected_session_id`, `seal_operation_id`, `seal_request_sha256`, `seal_receipt_id`, `seal_receipt_sha256`, `sealed_inventory_sha256`, `restore_launch_id`, `restore_request_sha256`, `restore_result_id`, `restore_result_sha256`, `destination_descriptor_sha256`, `destination_runtime_id`, `destination_runtime_incarnation`, `destination_guest_boot_id`, `destination_enrollment_revision`, `reserved_session_id`. All appear inside the single signed transition request, not independently replaceable lookup hints.

Require `restore_launch_id=launch_id=LR`, bridge A equal to the saved predecessor epoch, bridge C equal to the destination reserved session, and destination coordinates equal to the main tagged binding. Both descriptors belong to the same captured tenant/family/sandbox transition. The lifecycle issuer proves the LR request/result and saved predecessor relation; the node verifies the exact first seal evidence against the authoritative predecessor enrollment and exact destination result/enrollment. A missing/unavailable predecessor proof keeps the result incomplete. A same-runtime wake uses the exact same-enrollment first seal and has no restore bridge. Receipt validation checks every bridge field and its signed digest, not only the outer sandbox ID.

Extend ReleaseEpoch with `publication_revision`, `access_grant_revision`, `release_revision`, `expected_provider_revision`, and `lease_not_after_unix_ms` (integer UTC milliseconds, copied from the saved policy grant), alongside the existing installation/bind/grant IDs and hashes. Persist the canonical bytes/deadline in the release claim. The node compares expected revision/current enrollment under its host gate, fsyncs the accepted release and fixed deadline before admission, and uses an anchored monotonic countdown that cannot outlive that deadline. Clock uncertainty or restart requires authoritative deadline revalidation and keeps admission closed meanwhile; a retry never restarts a relative lease duration.

Renewal includes `previous_release_operation_id`, `previous_release_receipt_sha256`, `expected_provider_revision`, next `release_revision`, the currently revalidated publication/grant revision and a new policy-derived absolute deadline. Only the exact prior completed release with no unresolved close/revocation may renew. A new operation cannot rewrite the old receipt or deadline. Revocation stores a terminal fence for that exact enrollment/publication; a later revision alone cannot resurrect it. Any future reopening requires its own explicitly authorized transition, not renewal.

In the revocation-first ordering, the node's committed fence rejects a previously signed delayed release before forwarding/admission. In the release-first ordering, revocation closes admission and retains all already admitted work in its fence inventory; it acknowledges conclusive closure only with the required independent host evidence. During unknown fencing, retain running-work accounting and deny replacement publication. Recovered release evidence remains historical unless current enrollment/publication/grant reproof permits disclosure. Neutral lookup does not send a new ReleaseEpoch or renew a lease.

### Confirmation boundary and tests

I accept codex-9's proposed private participants `captureInitialProcessAdoptionProofTx`, `installProcessEpochReceiptTx`, `claimProcessEpochReleaseTx`, and `completeProcessEpochReleaseTx` as the lifecycle owners of these operations. SQL handles remain private; no provider code becomes another billing writer. The issuer consumes committed saved claims, not caller DTOs. The release/access-grant proof and restore bridge must be frozen in the joint contract and canonical vectors before composition.

Add golden vectors and real tests for initial-with-launch rejection, transition-without-launch rejection, swapped authority arms, changed original owner/adoption binding, A-seal/destination-C mismatches, changed LR request/result, duplicate release with a changed deadline, reordered/late renewal, and both delayed-release/revocation orders. Creation evidence must open billing at P1 before access publication; tests prove bound-closed time is not free. Full-row/ledger assertions prove replay does not reopen or move billing start. Neutral lookup after deactivation retains exact evidence without any signing/dispatch of a user grant. Unknown host fence preserves obligations and cannot count as a valid seal.

This is agreement on the eight lifecycle positions and the provider's concrete encoding proposal for the remaining text. It is not `STATUS CONTRACT CONFIRMED`: codex-9 still writes the consolidated version, codex-7 gates it, and both owners must confirm that exact committed contract. No implementation started in this reply.
